services:
  app:
    build: ./FlaskApp
    depends_on:
      - db
    ports:
      - "5002:5002"

  db:
    image: mysql:latest
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - ./MySQL_Queries:/docker-entrypoint-initdb.d/:ro